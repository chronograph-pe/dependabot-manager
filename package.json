{
  "name": "dependabot-manager",
  "version": "1.0.0",
  "description": "GitHub Action to intelligently manage Dependabot alerts",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test src/__tests__/*.test.js",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  }
}
